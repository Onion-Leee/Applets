<include src="../templates/header"/>





武汉加油！
<button type="default" size="{{defaultSize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="default" hover-class="other-button-hover"> default </button>
<button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="primary"bindtap="btnClik">{{btnText}} </button>
<view wx:if="{{show}}">{{text}}1</view>
<view wx:else>{{text}}2</view>   

<view wx:for="{{new}}"wx:for-item="itemx" wx:for-index="ix">

{{ix}}-{{itemx}}


</view>


<import src="../templates/footer"/>
<template is=footer1" data="{{text:'导入设置的内容'}}"/>